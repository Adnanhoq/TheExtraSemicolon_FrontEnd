{% block links %}
    <link rel="stylesheet" href="/css/halloweenTheme.css">
{% endblock %}

    <div class="d-flex justify-content-center mt-4">
        <div class="d-flex flex-column justify-content-center w-25">

        {% if errormessage %}
            <h3 class="error-message mb-4 mt-4">{{ errormessage }}</h3>
        {% endif %}
        <h1 class="mb-4 halloween-text">Your Profile</h1>
        <h3 class="mb-2 halloween-text">
        Email:
        </h3>
        <p class="halloween-text" >{{ email }}</p>
        <a class="mt-3" href="/profile/picture">Update Profile Picture</a>
        </div>

    </div>
